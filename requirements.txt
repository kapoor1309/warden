# Band SDK. Real package is `band-sdk` (1.0.0); top-level import is `band`.
# Framework-adapter extras (e.g. [langgraph]) are unverified — add them when
# wiring the live crew. The U1 spike uses the built-in SimpleAdapter, no extras.
band-sdk

# Core
python-dotenv
httpx
pyyaml

# LLM frameworks. Only Intake/Investigator use an LLM (LangGraph ReAct via the
# band LangGraph adapter, which imports langchain.agents.create_agent). Matcher,
# Approver, Warden, Enforcer, Payer are no-LLM SimpleAdapters and need none of these.
langchain
langchain-openai
langgraph
crewai
anthropic

# Attack benchmarks (real, third-party — CLAUDE.md rule). agentdojo provides the
# Banking-suite injection goals pulled live in attacks/benchmarks.py; InjecAgent
# strings are vendored under attacks/payloads/ with attribution.
agentdojo

# Tests (offline)
pytest
